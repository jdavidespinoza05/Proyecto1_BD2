events { worker_connections 1024; }

http {
    upstream api_servers {
        server api:8080; 
    }

    server {
        listen 80;

        location /api/ {
            proxy_pass http://api_servers;
        }

        location /search/ {
            return 503 "Servicio de busqueda en construccion";
        }
    }
}